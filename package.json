{
  "name": "research-vault",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "postinstall": "npm run db:generate -w @research-vault/api",
    "setup": "node scripts/setup.mjs",
    "dev": "cross-env SWAGGER_ENABLED=1 concurrently -n api,web -c blue,green \"npm run dev -w @research-vault/api\" \"npm run dev -w @research-vault/web\"",
    "start": "npm run dev",
    "build": "npm run build -w @research-vault/api && npm run build -w @research-vault/web",
    "db:generate": "npm run db:generate -w @research-vault/api",
    "db:push": "npm run db:push -w @research-vault/api",
    "db:migrate": "npm run db:migrate -w @research-vault/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3"
  }
}
